#include <iostream>

using namespace std;

int minStepsToReachHouse(int x) {
    
    int steps = x / 5;
    if (x % 5 != 0) {
        steps++;
    }
    return steps;
}

int main() {
    int x;
    cin >> x;

    cout << minStepsToReachHouse(x) << endl;

    return 0;
}

